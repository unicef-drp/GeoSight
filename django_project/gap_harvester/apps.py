"""App config for gap_harvester."""
from django.apps import AppConfig


class Config(AppConfig):
    """App config for gap_harvester."""

    name = 'gap_harvester'
    verbose_name = "GAP Harvester"
